"""Headless batch-sweep package (pdd.md sections 10, 11.3).

Stub created by the scaffold. The batch agent implements sweeps here (e.g. swarm-size
sweep for the breakeven curve, gap-vs-scale), reading sweep specs via
``beam.config.load_sweep`` and producing artifact series.
"""
