# Backend WebSocket/API URL
# Override when the backend runs on a different host or port than the frontend.
# Leave unset to use the page origin (works when the backend is proxied or co-hosted).
VITE_API_URL=http://localhost:8000
