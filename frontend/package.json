{
  "name": "beam-frontend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "BEAM frontend — Pixi.js battlefield, control panel, gap + breakeven dashboards, solver-race split view.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "pixi.js": "^8.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0",
    "vitest": "^1.6.0"
  }
}
